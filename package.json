{
  "name": "openguarana",
  "private": true,
  "version": "0.1.0",
  "description": "A secure, team-native fork of OpenClaw",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build":     "pnpm -r run build",
    "test":      "vitest run",
    "test:ui":   "vitest --ui",
    "lint":      "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "typescript":          "^5.4.0",
    "vitest":              "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
